Playwright UI test skeleton

Instructions:

- Install dependencies: `npm install` (from `tests/playwright` folder).
- Run tests: `npm test`.

This folder contains a minimal, editable test that navigates the app UI. Update selectors and credentials to match your environment.
